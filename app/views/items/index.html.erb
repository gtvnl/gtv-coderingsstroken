<% breadcrumb :items, @items %>

<h1>Modulen</h1>

<table>
  <thead>
    <tr>
      <th>Nummer:</th>
      <th>Titel:</th>
      <th>Omschrijving:</th>
      <th>Aantal Modulen:</th>
      <th>Regel:</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= link_to item.number, edit_page_line_item_path(item.line.page,item.line,item) %></td>
        <td><%= link_to item.title, edit_page_line_item_path(item.line.page,item.line,item) %></td>
        <td><%= item.description %></td>
        <td><%= item.modulen %></td>
        <td><%= item.line.number %></td>
        <td>
          <%= link_to page_line_item_path(item.line.page,item.line,item), method: :delete, data: { confirm: 'Are you sure?' } do %><i class="fi-trash"></i><% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nieuwe module', new_page_line_item_path, class: 'button' %>
